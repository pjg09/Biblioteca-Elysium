spring:
  application:
    name: cli-service

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
  instance:
    hostname: localhost
    prefer-ip-address: false

# URLs de los servicios (se pueden sobrescribir con variables de entorno)
materiales:
  service:
    url: http://localhost:8082

usuarios:
  service:
    url: http://localhost:8083

multas:
  service:
    url: http://localhost:8084

reservas:
  service:
    url: http://localhost:8088

circulacion:
  service:
    url: http://localhost:8081
